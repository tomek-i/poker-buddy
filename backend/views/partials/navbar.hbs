<!--Navbar -->
<nav class="navbar navbar-icon-top navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">Poker Buddy</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item  {{#if activeHome }}active{{/if}}">
                <a class="nav-link" href="/">
                    <i class="fa fa-home"></i>
                    Home
                    {{#if activeHome }}
                    <span class="sr-only">(current)</span>
                    {{/if}}
                </a>
            </li>
            {{#if user}}
            {{else}}

            <li class="nav-item">
                <a class="nav-link {{#if activeFeatures }}active{{/if}}" href="/features">
                    <i class="fa fa-cogs"></i>
                    Features
                    {{#if activeFeatures }}
                    <span class="sr-only">(current)</span>
                    {{/if}}
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link {{#if activeAbout }}active{{/if}}" href="/about">
                    <i class="fa fa-info"></i>
                    About
                    {{#if activeAbout }}
                    <span class="sr-only">(current)</span>
                    {{/if}}
                </a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link {{#if activeContact }}active{{/if}}" href="/contact">
                    <i class="fa fa-envelope-o"></i>
                    Contact
                    {{#if activeContact }}
                    <span class="sr-only">(current)</span>
                    {{/if}}
                </a>
            </li>
            {{/if}}
        </ul>
        <ul class="navbar-nav ">
            {{#if user}}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" , data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    <i class="fa fa-user">
                        <span class="badge badge-danger">11</span>
                    </i>
                    Profile
                </a>
                <div class="dropdown-menu" aria-labelledby="userDropdown" style="left:-100%">
                    {{#if user.isAdmin}}
                    <a class="dropdown-item" href="/admin">Admin Area</a>
                    {{/if}}

                    <a class="dropdown-item" href="#">Another action</a>
                    <div class="dropdown-divider"></div>
                    <form action="/logout" method="post">
                        <button class="dropdown-item" href="#">Logout</button>
                    </form>
                </div>
            </li>
            {{else}}
            <li class="nav-item">
                <a class="nav-link" href="/login">
                    <i class="fa fa-sign-in"></i>
                    Login
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">
                    <i class="fa fa-globe">
                        <span class="badge badge-success">11</span>
                    </i>
                    Test
                </a>
            </li>
            {{/if}}

        </ul>

    </div>
</nav>
<!--/.Navbar -->